# Issues

## 2026-01-27
- `pnpm run lint` currently fails due to parse errors in `src/main/ipc/agent.ts` and other lint rules.
### Unchecked Checkboxes Search (2026-01-27 22:33:34)

Searched for markdown files containing unchecked checkboxes (`- [ ]` pattern).
Commands used:
`glob(pattern='**/*.md')`
`grep(pattern='^-\s*\[\s*\]', include='**/*.md', path='.')`

Result: No files with the specified pattern were found.

